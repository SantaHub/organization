name: Jekyll Test
on:
  push:
    branches:
      - main
  pull_request:
    types: [ opened, synchronize, reopened, edited, ready_for_review ]

jobs:
  printMine:
    name: call production
    runs-on: ubuntu-latest
    steps:
      - name: Print Secret
        run: echo ${{ secrets.MY_NUMBER }}*11

  callProduction:
    uses: SantaHub/organization/.github/workflows/blank.yml@main
    